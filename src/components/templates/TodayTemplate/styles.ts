import styled from "styled-components/native";

const Area = styled.View`
    flex: 1;
    row-gap: 24px;
    margin: 24px 0;
    padding-bottom: 78px; 
`;

const Top = styled.View`
    padding: 0 16px;
`;

const Section = styled.View`
    padding: 16px;
    row-gap: 32px;
`;

const Scroll = styled.ScrollView``;

export { Area, Top, Section, Scroll }