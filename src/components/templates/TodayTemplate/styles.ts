import styled from "styled-components/native";

const Area = styled.View`
    flex: 1;
    row-gap: 24px;
    margin: 24px 0;
`;

const Top = styled.View`
    padding: 0 16px;
`;

const Section = styled.View`
    padding: 16px;
    row-gap: 32px;
`;


export { Area, Top, Section }